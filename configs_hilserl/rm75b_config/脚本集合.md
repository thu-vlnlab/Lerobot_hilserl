1. 数据采集：
    python -m lerobot.rl.gym_manipulator --config_path configs_hilserl/rm75b_config/test_config.json 
    enter：完成但不结束   baskspace：删除数据 q：完成结束 
2. 数据可视化：
    python -m lerobot.scripts.lerobot_dataset_viz   --repo-id  1   --root /home/ubuntu/Lerobot_hilserl/data/rm75_reward_data_collection  --episode-index 2
3. 训练reward：python -m lerobot.scripts.lerobot_train --config configs_hilserl/rm75b_config/reward_classifier_train.json


4. 可视化
已有数据集上回放视频并显示预测结果，对比 Ground Truth。                                                                                      
 # 1. 训练模型
  python -m lerobot.scripts.lerobot_train --config configs_hilserl/rm75b_config/reward_classifier_train.json

  # 2. 在数据集上验证（可视化 + 统计）
  python scripts/visualize_dataset_predictions.py \
      --model-path outputs/rm75_reward_classifier \
      --data-path data/rm75_reward_data_collection \
      --scale 3.0

  # 3. 快速统计（不开窗口）
  python scripts/visualize_dataset_predictions.py \
      --model-path outputs/rm75_reward_classifier \
      --data-path data/rm75_reward_data_collection \
      --fast

  # 4. 实时相机推理
  python scripts/realtime_reward_classifier.py \
      --model-path outputs/rm75_reward_classifier \
      --realsense --serial 352122273493

  # 5. 数据集精度测试
  python scripts/test_reward_classifier.py
      